- name: external file variables
  hosts: local
  connection: local
  vars_files:
    - 04_vars.yaml
  tasks:
    - name: print external file variables
      ansible.builtin.debug:
        msg: "course is {{course}} and trainer is {{trainer}}"


  # Task level variables if present will always override vars_file level variables